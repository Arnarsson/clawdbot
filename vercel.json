{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm ui:install && pnpm ui:build",
  "outputDirectory": "dist/control-ui",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
